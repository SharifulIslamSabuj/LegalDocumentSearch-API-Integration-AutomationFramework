# ================================
# CORE CONFIG
# ================================
env=qa

# ================================
# ENVIRONMENT-SPECIFIC URLs
# ================================
dev.base.url=http://localhost:8000
qa.base.url=http://localhost:8000
prod.base.url=http://localhost:8000

# ================================
# TIMEOUT CONFIG (in milliseconds)
# ================================
connection.timeout=5000
read.timeout=10000

# ================================
# RETRY CONFIG
# ================================
retry.count=2
retry.delay=1000
